version: '3.9'

services:
  postgresql:
    image: postgres:16.2
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: worldOfJavacraft
